spring:
  config:
    import: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  rabbitmq:
    host: localhost
    port: 5672
  mail:
    host: localhost
    port: 25

eureka:
  client:
    enabled: false
